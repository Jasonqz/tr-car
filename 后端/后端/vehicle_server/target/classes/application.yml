server:
  port: 9000
  address: 127.0.0.1
#数据库连接配置
spring:
  application:
    name: vehicle_server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db01
    username: root
    password: 123456
  #文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
#mybatis配置
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
#阿里云OSS配置
aliyun:
  oss:
    endpoint: dadoajdijasiod
    accessKeyId: djaojdpaopsda
    accessKeySecret: ajdaojdpajpda
    bucketName: jdoajdpajpod
#开启spring事务管理日志
logging:
  level:
    org.springframework.jdbc.support.JdbcTransactionManager: debug